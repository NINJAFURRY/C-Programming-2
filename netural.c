#include<stdio.h>
int main () {
    int x;
    printf("Enter a number to to check it's a netural number :");
    scanf("%d",&x);
    if (x>0) {
        printf("it's an netural number");
    }
    else {
        printf("it's not an netural number");
    }
}